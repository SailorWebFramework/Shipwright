
// This file was autogenerated by Shipwright. DO NOT CHANGE.
//
//  Tailwind+Classes.swift
//  
//  Created by Joshua Davis on 10/6/23.
//

public enum TailwindClass: Equatable, CustomStringConvertible {
    {{#cases}}
    ///{{description}}
    {{#isPack}}
    case `{{alias}}`([TailwindPack])
    {{/isPack}}
    {{^isPack}}
    case `{{alias}}`
    {{/isPack}}
    
    {{/cases}}
    public var description: String {
        switch self {
        {{#cases}}
        {{#isPack}}
        case .{{alias}}(let classes):
            return "{{name}}\(classes.description)"
        {{/isPack}}
        {{^isPack}}
        case .{{alias}}:
            return "{{name}}"
        {{/isPack}}
        {{/cases}}
        }
    }
}

public enum TailwindPack: Equatable, CustomStringConvertible {
    {{#packables}}
    ///{{description}}
    case `{{alias}}`
    
    {{/packables}}
    public var description: String {
        switch self {
        {{#packables}}
        case .{{alias}}:
            return "{{name}}"
        {{/packables}}
        }
    }
}