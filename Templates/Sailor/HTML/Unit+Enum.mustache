// This file was autogenerated by Shipwright. DO NOT CHANGE.
//
//  Unit+{{cname}}.swift
//  
//  Created by Joshua Davis on 10/6/23.
//


extension Unit {
    public enum {{cname}}: StyleValue, Equatable {
        public var description: String {
            switch self {
            case .hex(let hexcode):
                return hexcode
            case .rgb(let r, let g, let b):
                return "rgb(\(r), \(g), \(b))"
            case .rgba(let r, let g, let b, let a):
                return "rgb(\(r), \(g), \(b), \(a))"
            case .hsl(let h, let s, let l):
                return "hsl(\(h), \(s)%, \(l)%)"
            case .hsla(let h, let s, let l, let a):
                return "hsl(\(h), \(s)%, \(l)%, \(a))"
            case .empty:
                return ""
            }
            switch self {
            {{#cases}}
            case .{{name}}{{#names}}(let {{.}}{{^last},{{/last}}}){{/names}}:
                return "{{name}}({{#associatedValues}}\({{.}}){{/associatedValues}})"
            {{/cases}}
            }
        }
        
        {{#cases}}
        case {{name}}{{#values}}({{.}}){{/values}}
        {{/cases}}

    }
    
}