// This file was autogenerated by Shipwright. DO NOT CHANGE.
//
//  Unit+{{cname}}.swift
//  

extension Unit {
    public struct {{cname}}: Equatable, CustomStringConvertible {
        var value: String

        public var description: String {
            self.value
        }

        init(_ value: String) {
            self.value = value
        }

        {{#cases}}
        ///{{description}}
        {{#hasAssociatedValue}}
        public static func `{{alias}}`({{#args}}{{^isShown}}_ {{/isShown}}{{name}}: {{value}}{{^last}}, {{/last}}{{/args}}) -> Self {
            {{#isFormatted}}
            return .init("{{{format}}}")
            {{/isFormatted}}
            {{^isFormatted}}
            return .init("{{name}}({{#args}}\({{name}}){{^last}} {{/last}}{{/args}})")
            {{/isFormatted}}
        }
        {{/hasAssociatedValue}}
        {{^hasAssociatedValue}}
        public static var `{{alias}}`: Self {
            {{#isFormatted}}
            return .init("{{{format}}}")
            {{/isFormatted}}
            {{^isFormatted}}
            return .init("{{name}}")
            {{/isFormatted}}
        }
        {{/hasAssociatedValue}}

        {{/cases}}

    }
}
