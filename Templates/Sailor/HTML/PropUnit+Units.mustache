// This file was autogenerated by Shipwright. DO NOT CHANGE.
//
//  Unit+{{cname}}.swift
//  

extension PropUnit {
    public struct {{cname}} {
        var value: String

        public var description: String {
            self.value
        }

        init(_ value: String) {
            self.value = value
        }

        {{#cases}}
        ///{{description}}
        {{#hasAssociatedValue}}
        public static func `{{alias}}`({{#args}}_ {{name}}: {{value}}{{^last}}, {{/last}}{{/args}}) -> Self {
            {{#isFormatted}}
            return .init("{{format}}")
            {{/isFormatted}}
            {{^isFormatted}}
            return .init("{{name}}({{#args}}\({{name}}){{^last}} {{/last}}{{/args}})")
            {{/isFormatted}}
        }
        {{/hasAssociatedValue}}
        {{^hasAssociatedValue}}
        public static var `{{alias}}`: Self {
            {{#isFormatted}}
            return .init("{{format}}")
            {{/isFormatted}}
            {{^isFormatted}}
            return .init("{{name}}")
            {{/isFormatted}}
        }
        {{/hasAssociatedValue}}

        {{/cases}}

    }
}
