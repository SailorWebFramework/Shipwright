// This file was autogenerated by Mustache. DO NOT CHANGE.

import JavaScriptKit

extension Page {

    public func on{{name}}(_ completion: @escaping ({{#istyped}}{{ctype}}{{/istyped}}) -> Void) -> any Page {
        guard var copy = self as? any HTMLElement else {
            return Div { self }.on{{name}} { {{#istyped}}value in{{/istyped}}
                completion({{#istyped}}value{{/istyped}})
            }
        }
        {{#istyped}}
        copy.events["{{lname}}"] = Event(
            name: "{{lname}}", 
            action: { eventResult in
                if case let EventResult.{{ltype}}(value) = eventResult {
                    completion(value)
                }
            }
        )
        {{/istyped}}
        {{^istyped}}
        copy.events["{{lname}}"] = Event(name: "{{lname}}", action: { _ in
            completion()
        })
        {{/istyped}}

        return copy
    }

}